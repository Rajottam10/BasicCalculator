apiVersion: apps/v1
kind: Deployment
metadata:
  name: basic-calculator
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: basic-calculator
  template:
    metadata:
      labels:
        app: basic-calculator
    spec:
      containers:
        - name: basic-calculator
          image: 172.26.186.152:8081/library/basic-calculator:IMAGE_TAG_TO_REPLACE
          ports:
            - containerPort: 8080

