apiVersion: apps/v1
kind: Deployment
metadata:
  name: basic-calculator
  namespace: basic-calculator
spec:
  replicas: 1
  selector:
    matchLabels:
      app: basic-calculator
  template:
    metadata:
      labels:
        app: basic-calculator
    spec:
      containers:
        - name: basic-calculator
          image: host.docker.internal:8081/library/basic-calculator:latest
          ports:
            - containerPort: 8080


