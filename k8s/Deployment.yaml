apiVersion: apps/v1
kind: Deployment
metadata:
  name: basic-calculator
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: basic-calculator
  template:
    metadata:
      labels:
        app: basic-calculator
    spec:
      containers:
        - name: basic-calculator
          image: harbor.your-domain.com/rajottam/basic-calculator:IMAGE_TAG_TO_REPLACE
          ports:
            - containerPort: 8080
